<% theme = Theme.current_theme %>

<% if theme&.favicon_image&.attached? %>
  <%= favicon_link_tag(Rails.application.routes.url_helpers.rails_blob_path(theme&.favicon_image, {only_path: true})) %>
<% else %>
  <%= favicon_link_tag('default_favicon.ico') %>
<% end %>